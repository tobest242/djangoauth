from django.urls import path, include
from rest_framework import routers
from blog.views import PostViewSet

urlpatterns = [
    # path('all-post', PostView.as_view()),
    # path('new-post', AddPostView.as_view())
]